#lang gibbon

(data Foo (A Int) (B Int))

(define (foo [ev : Foo]) : Foo
  (case ev
    [(A x) (B x)]
    [(B y) ev]))
    ; [(B y) (A y)]

(foo (A 1))

;; This will make --packed fail currently:
; (foo (B 33))
