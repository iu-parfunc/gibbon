#lang gibbon

(require "../../ASTBenchmarks/grammar_racket.gib")


(let ([top : Toplvl (BeginTop (CONSTOPLVL (Expression (VARREF (quote hello)))
                                      (NULLTOPLVL)))])
  0)
