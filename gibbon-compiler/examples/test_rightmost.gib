#lang gibbon

(require "test_ddtree.gib")
(require "test_buildtree.gib")

(define (rightmost [tr : Tree]) : Int
  (case tr
    [(Leaf n) n]
    [(Node x y) (rightmost y)]))

(rightmost (buildtree 5))
