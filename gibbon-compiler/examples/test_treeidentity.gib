#lang gibbon

(require "test_ddtree.gib")
(require "test_buildtree.gib")

(define (id [tr : Tree]) : Tree
  tr)

(let ([tr0 : Tree (buildtree 10)])
  (let ([tr1 : Tree (iterate (let ([tr2 : Tree (id tr0)])
                               tr2))])
    10))
